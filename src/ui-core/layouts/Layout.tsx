import { Header } from "../components";
import { OutletContainer } from "../components";

function Layout() {
	return (
		<div>
			<Header />
			<OutletContainer />
		</div>
	);
}

export default Layout;
